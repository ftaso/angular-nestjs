import { NumHydratePipe } from './num-hydrate.pipe';

describe('NumHydratePipe', () => {
  it('create an instance', () => {
    const pipe = new NumHydratePipe();
    expect(pipe).toBeTruthy();
  });
});
