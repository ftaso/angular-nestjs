import { SelectedStaffPipe } from './selected-staff.pipe';

describe('SelectedStaffPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedStaffPipe();
    expect(pipe).toBeTruthy();
  });
});
