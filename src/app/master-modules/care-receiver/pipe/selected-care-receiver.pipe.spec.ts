import { SelectedCareReceiverPipe } from './selected-care-receiver.pipe';

describe('SelectedCareReceiverPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedCareReceiverPipe();
    expect(pipe).toBeTruthy();
  });
});
