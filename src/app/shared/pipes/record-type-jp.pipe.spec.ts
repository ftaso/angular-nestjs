import { RecordTypeJpPipe } from './record-type-jp.pipe';

describe('RecordTypeJpPipe', () => {
  it('create an instance', () => {
    const pipe = new RecordTypeJpPipe();
    expect(pipe).toBeTruthy();
  });
});
