import { BooleanToCirclePipe } from './boolean-to-circle.pipe';

describe('BooleanToCirclePipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanToCirclePipe();
    expect(pipe).toBeTruthy();
  });
});
